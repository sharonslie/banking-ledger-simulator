# banking-ledger-simulator
C++ banking ledger simulation using a global pointer array of account structs with linked relationships. Implements populate(), withdraw() with fee logic for shared-access and standard/premium accounts, and display_ledger() with formatted multi-account output.
